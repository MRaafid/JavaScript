let animals = ['ape', 'bat', 'cat', 'dog'];
animals.push('elephant'); // add the string at the end og the array
console.log(animals[2]) // gets the 2+1 i.e 3rd element of the array
console.log(animals);
animals.pop();    // delets the last elemet of the array
console.log(animals);